"""Fortran namelist read/write (vendored from runner.ext.namelist).

Provides the namelist file format plus the ``param_write_to_files`` and
``param_summary`` helpers that the existing runme script already relies on.

Vendored verbatim in Phase 3 (Python 3 only; drop ``six`` / ``__future__``).
"""
